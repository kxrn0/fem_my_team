import styled from "styled-components";

const SCTopic = styled.div`
  color: var(--white);
  display: flex;
  align-items: center;
  gap: 25px;

  svg {
    min-width: 72px;
  }
`;

export default SCTopic;
